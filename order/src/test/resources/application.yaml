spring:
  datasource:
    url: jdbc:h2:mem:ordertestdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

services:
  order:
    internal-token: test-order-token

eureka:
  client:
    enabled: false
